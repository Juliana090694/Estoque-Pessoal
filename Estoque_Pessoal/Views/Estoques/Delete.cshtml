@model Estoque_Pessoal.Models.Estoque

@{
    ViewBag.Title = "Delete";
}

<h2>Excluir</h2>

<h3>Tem certeza que deseja remover do estoque?</h3>
<div>
    <h4>Meu estoque</h4>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.EstoqueItem.Item.Nome)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.EstoqueItem.Item.Nome)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.EstoqueItem.Quantidade)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.EstoqueItem.Quantidade)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Excluir" class="btn btn-default" /> |
            @Html.ActionLink("Voltar ao estoque", "Index")
        </div>
    }
</div>
